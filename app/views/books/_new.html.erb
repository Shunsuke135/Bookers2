
    <%= form_with model: book, local:true do |f| %>
      <% if book.errors.any? %>
       <h3><%= book.errors.count %>prohibited this obj from being saved:</h3>
        <ul>
         <% book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
         <% end %>
        </ul>
      <% end %>

        <p>Title</p>
        <%= f.text_field :title %>
        <p>Opinion</p>
        <%= f.text_area :body %><br>
        <%= f.submit "Create Book", class:"btn btn-success w-178px" %><br>
    <% end %>
